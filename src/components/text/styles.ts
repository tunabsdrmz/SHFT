import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  Container: {
    marginTop: 15,
  },
  Text: {
    fontSize: 20,
    fontWeight: '700',
    color: '#378CA7',
    textAlign: 'center',
  },
});
