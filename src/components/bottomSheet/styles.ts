import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  Seperator: {
    borderTopWidth: 0.5,
    borderRadius: 10,
    borderColor: '#378CA7',
  },
  IndicatorStyle: {
    backgroundColor: '#378CA7',
    width: 100,
  },
});
