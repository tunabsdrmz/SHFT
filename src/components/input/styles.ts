import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  Container: {
    marginRight: 'auto',
    marginLeft: 'auto',
    marginTop: 25,
  },
  Input: {
    width: 150,
    backgroundColor: '#FFF',
    borderRadius: 10,
    padding: 10,
    fontSize: 24,
    fontWeight: '700',
    textAlign: 'center',
  },
});
