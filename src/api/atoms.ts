import {atom} from 'jotai';

export const goalDataId = atom('1');
export const valueAtom = atom('');
export const activeButtonId = atom(1);
export const recommendText = atom('You should drink more water!');
